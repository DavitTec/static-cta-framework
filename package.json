{
  "name": "static-cta-framework",
  "version": "0.1.0",
  "type": "module",
  "description": "minimal but real, working skeleton for Static Website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "node scripts/clean.js",
    "watch": "node scripts/watch.js",
    "sass": "sass src/sass:dist/css --no-source-map",
    "render": "node scripts/render.js",
    "copy": "node scripts/copy.js",
    "build:dev": "pnpm clean && pnpm sass && pnpm render && pnpm copy",
    "serve": "live-server dist",
    "dev": "pnpm build:dev && pnpm serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "dependencies": {
    "dotenv": "^16.4.0",
    "nunjucks": "^3.2.4"
  },
  "devDependencies": {
    "chokidar": "^5.0.0",
    "sass": "^1.70.0"
  }
}
